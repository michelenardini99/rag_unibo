# --- Ambiente Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Configurazione locale / segreti ---
# .env contiene valori locali (porte, device GPU, eventuale token HF);
# .env.example resta tracciato come template.
.env

# --- Cache pesi Hugging Face (Docling, BGE-M3, reranker, VLM) ---
.cache/

# --- Corpus documentale ---
# Documenti d'Ateneo (dati sensibili, vedi architettura §"vincoli non negoziabili")
# e relative conversioni/artefatti: mai in git, restano solo in locale.
datasets/

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Claude Code ---
.claude/settings.local.json

chainlit.md
.chainlit/
